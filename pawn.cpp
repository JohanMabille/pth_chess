#include "pawn.hpp"
#include <iostream>

namespace dauphine
{
 
   pawn::pawn(std::size_t row, std::size_t col)
	: m_row(row), m_col(col)
   {
	
   }


   bool can_move(std::size_t row, std::size_t col)
   {

   }


}









































